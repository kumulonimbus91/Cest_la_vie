<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nenad.cestlavieuzice">

<!--    <uses-permission android:name="android.permission.WAKE_LOCK" />-->

<!--    <uses-feature android:name="android.hardware.type.watch" />-->

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CestLaVieUzice">


        <activity android:name=".view.activities.ForgotPasswordActivity" />
        <activity android:name=".view.activities.LogInActivity" />
        <activity android:name=".view.activities.SignUpActivity" />
        <activity android:name=".view.activities.IntroScreen" />
        <activity android:name=".view.activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activities.MainActivity" />
    </application>

</manifest>